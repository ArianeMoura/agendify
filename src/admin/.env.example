# URL raiz da API do Agendify (sem /api no final — o cliente acrescenta).
# Local:   http://localhost:5089   (rode a API .NET: cd src/api && dotnet run)
# Deploy:  https://agendify-api-j6da.onrender.com   (API publicada no Render)
#
# Copie para .env.local (ignorado pelo git) e ajuste conforme o ambiente.
NEXT_PUBLIC_API_URL=https://agendify-api-j6da.onrender.com
